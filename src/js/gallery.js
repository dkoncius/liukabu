import Swiper from 'swiper/bundle';
import 'swiper/css/bundle';

// Initialize Swiper
const swiper = new Swiper('.swiper-container', {
   // Customize your Swiper settings here
   slidesPerView: 1,
   spaceBetween: 20,
   loop: true, // Enable loop mode if needed
   // Add more options as required
});
